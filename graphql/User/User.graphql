type User {
    id: ID!
    name: String!
    email: String!
    avatar: String
    created_at: DateTime!
    updated_at: DateTime!
    password: String! @middleware(checks:["auth:api"])
    books: [Book] @belongsToMany
    pivot: BookUserPivot
}
