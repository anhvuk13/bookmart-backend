extend type Mutation {
  updateReview(input: UpdateReviewInput! @spread): User @update
}

input UpdateReviewInput {
  id: ID!
  books: UpdateUserReviews
}

input UpdateUserReviews {
  sync: [ConnectUserReview!]
}

input ConnectUserReview {
  id: ID!
  review: String
  rating: Int
}