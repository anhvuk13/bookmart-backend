type Book {
    id: ID!
    name: String!
    author: String!
    image: String
    link: String
    categories: [Category] @belongsToMany
    categoriesArray: [Category]
    users: [User] @belongsToMany
    pivot: BookUserPivot
}
